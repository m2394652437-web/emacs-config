# -*- mode: snippet -*-
# name: new_class_header
# key: ch
# --
#ifndef `(upcase (file-name-base (buffer-file-name)))`_H
#define `(upcase (file-name-base (buffer-file-name)))`_H

class `(file-name-base (buffer-file-name))`
{
private:	
public:
    $0
};

#endif
